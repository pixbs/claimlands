[package]
name = "xtask"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

# Deliberately dependency-free. This is the tool that guards the dependency
# graph, so it should not have one of its own — and CI runs it on every PR,
# where a fast cold build matters.
[dependencies]
